# Environment
NODE_ENV=development

# Server
PORT=3000
API_PREFIX=api

# Database (to be configured)
# DATABASE_URL=
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=user_story_mapping
# DATABASE_USER=
# DATABASE_PASSWORD=

# JWT (to be configured if auth is needed)
# JWT_SECRET=
# JWT_EXPIRATION=3600

# CORS
# CORS_ORIGIN=http://localhost:3000
